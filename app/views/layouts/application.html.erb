<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>WeatherWare</title>
	<%= stylesheet_link_tag    'application', media: 'all'%>
	<%= javascript_include_tag 'application' %>
	<%= csrf_meta_tags %>

</head>

<body>
	<header>
		<img src="/images/w-scarf-maroon.png" alt= "logo">
		<a href="<%= root_url %>"><h1 class="main-title">weather ware</h1></a>
		<hr>
		<h2>what to wear where you are</h2>

		<div class = "wrapper">
  <div class = "location-page"> 

    <div class= "location-form">
      <h1> weather where? </h1>

      <%= form_for :location, url: submit_url, html: {class: 'search-form'} do |f| %>
    
        <p class = "city-input">
          <%= f.label :City %>
          <%= f.text_field :city %>
        </p>

        <p class= "state-input">
          <%= f.label :State %>
          <%= f.select :state, options_for_select(us_states, "TX") %>
        </p>

        <p class= "submit">
          <%= f.submit 'Search' %>
        </p>
        <% end %>
      </div>
</div>
</div>


	</header>

	<%= yield %>
  
	<%= render "layouts/partials/footer" %>

</body>

</html>